<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="128dp"
    android:layout_height="match_parent"
    android:orientation="horizontal"
    android:gravity="right"
    android:theme="@style/Theme.HomeSwitchTheme">

    <ProgressBar
        android:id="@+id/progress_bar_shop_open_close"
        android:layout_width="24dp"
        android:layout_toLeftOf="@+id/switch_home_delivery_toggle"
        android:layout_height="24dp"
        android:layout_centerInParent="true"
        android:layout_marginRight="4dp"
        android:progressTint="@color/white"
        android:layout_gravity="center_vertical"
        android:indeterminate="true"
        android:visibility="gone"/>

    <android.support.v7.widget.SwitchCompat
        android:id="@+id/switch_shop_open_close"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_marginRight="8dp"
        android:fontFamily="sanf-serif-light"
        android:gravity="center_vertical"
        android:text="Open  "
        android:textColor="@color/white" />

</LinearLayout>